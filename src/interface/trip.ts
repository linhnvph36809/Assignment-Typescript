export interface  ITrip {
    busHouseId: string,
    startTime: string,
    fromStation?: string,
    toStation?: string,
    station: string,
    seats?: string,
    price?: string,
    stationId: string
}

