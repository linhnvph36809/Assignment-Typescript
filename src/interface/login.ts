export interface  ILogin {
    name: string,
    email: string,
    password: string,
    phone: string
}

export interface IBooking {
    tripId: string|null,
    name: string|null,
    email: string|null,
    phone: string|null
}

